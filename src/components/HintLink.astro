---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
	href: string;
}
const { href, ...attrs } = Astro.props;
---

<a href={href} target="_blank" rel="noopener noreferrer" class="hint" {...attrs}><slot /></a>
